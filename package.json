{
  "name": "compote",
  "version": "0.7.2",
  "license": "MIT",
  "main": "compote.js",
  "types": "compote.d.ts",
  "scripts": {
    "src:rollup": "rollup ./build/compote.js -o ./compote.js -g mithril:m -f es",
    "todomvc:rollup": "rollup ./build/examples/todomvc.js -o ./build/todomvc.js -g mithril:m -f es",
    "build": "tsc && npm run src:rollup && npm run todomvc:rollup",
    "start": "serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mithril": "1.0.1"
  },
  "devDependencies": {
    "@types/mithril": "0.0.31",
    "rollup": "0.41.4",
    "serve": "3.4.0",
    "typescript": "2.2.1"
  }
}
