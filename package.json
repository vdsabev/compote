{
  "name": "compote",
  "version": "0.8.0",
  "license": "MIT",
  "main": "compote.js",
  "types": "compote.d.ts",
  "scripts": {
    "compote:build": "rollup ./build/compote.js -o ./compote.js -g mithril:m -f es",
    "todomvc:build": "rollup ./build/examples/todomvc.js -o ./build/todomvc.js -g mithril:m -f es",
    "build": "tsc && npm run compote:build && npm run todomvc:build",
    "start": "serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mithril": "1.0.1"
  },
  "devDependencies": {
    "@types/mithril": "0.0.31",
    "rollup": "0.41.4",
    "serve": "3.4.0",
    "typescript": "2.1.6"
  }
}
